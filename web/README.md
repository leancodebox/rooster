# Rooster web

React, Vite, Tailwind CSS, and shadcn/ui dashboard for Rooster.

Run `pnpm dev` for frontend development against a daemon listening on port 9090.
Run `pnpm build` to generate the assets embedded by the Go application.

